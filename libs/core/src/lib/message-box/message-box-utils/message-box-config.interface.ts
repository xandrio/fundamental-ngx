import { DialogBaseConfig } from '../../dialog/dialog-base/dialog-base-config.interface';

// tslint:disable-next-line:no-empty-interface
export interface MessageBoxConfig extends DialogBaseConfig {
}
